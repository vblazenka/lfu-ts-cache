{
  "name": "lfu-ts-cache",
  "type": "module",
  "version": "1.0.0",
  "description": "Cache that uses eviction policy to remove least frequently used items.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "format": "prettier --write ."
  },
  "author": "Vedran Blazenka",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "vitest": "^0.32.0"
  }
}
